<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<insert id="member_insert" parameterType="member">
		insert into user_mem (idx, id, pw, name)
			values (
			#{idx},
			#{id},
			#{pw},
			#{name})
	</insert>
	
	
	<select id="member_select" parameterType="String" resultType="member">
		select * from user_mem where id = #{id}
	</select>
	
	
	
	
</mapper>